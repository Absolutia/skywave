# skywave
Chat program. Or its supposed to be. Doesn't really resemble one right now.
Currently for command line Linux/BSD only, but we have planned a DOS port as well as GUI versions for Windows & Android.
## libraries and stuff
It relies on the functionality of some non-standard libraries (more will likely be joining that list in the future) you will need to nab them to link skywave against.
